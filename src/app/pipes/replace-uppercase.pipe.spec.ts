import { ReplaceUppercasePipe } from './replace-uppercase.pipe';

describe('ReplaceUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
